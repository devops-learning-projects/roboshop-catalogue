name: Deploy - Dev
on:
  workflow_dispatch:
jobs:
  reuse:
    uses: devops-learning-projects/roboshop-github-actions/.github/workflows/deploy.yml@main
    with:
      app_name: catalogue
      app_env: dev
